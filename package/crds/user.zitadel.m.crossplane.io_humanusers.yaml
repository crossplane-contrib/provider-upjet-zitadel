---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: humanusers.user.zitadel.m.crossplane.io
spec:
  group: user.zitadel.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - zitadel
    kind: HumanUser
    listKind: HumanUserList
    plural: humanusers
    singular: humanuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HumanUser is the Schema for the HumanUsers API. Resource representing
          a human user situated under an organization, which then can be authorized
          through memberships or direct grants on other resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HumanUserSpec defines the desired state of HumanUser
            properties:
              forProvider:
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the user
                      Display name of the user
                    type: string
                  email:
                    description: |-
                      (String) Email of the user
                      Email of the user
                    type: string
                  firstName:
                    description: |-
                      (String) First name of the user
                      First name of the user
                    type: string
                  gender:
                    description: |-
                      (String) Gender of the user, supported values: GENDER_UNSPECIFIED, GENDER_FEMALE, GENDER_MALE, GENDER_DIVERSE
                      Gender of the user, supported values: GENDER_UNSPECIFIED, GENDER_FEMALE, GENDER_MALE, GENDER_DIVERSE
                    type: string
                  idpLinks:
                    description: |-
                      (Block List) A list of identity provider links to add to the user during creation. Useful for migration scenarios. (see below for nested schema)
                      A list of identity provider links to add to the user during creation. Useful for migration scenarios.
                    items:
                      properties:
                        idpId:
                          description: |-
                            (String) The ID of the identity provider.
                            The ID of the identity provider.
                          type: string
                        userId:
                          description: |-
                            (String) The ID of this resource. Optionally set a custom unique ID. If omitted, ZITADEL will generate one.
                            The user ID at the identity provider.
                          type: string
                        userName:
                          description: |-
                            (String) Username
                            The username at the identity provider.
                          type: string
                      type: object
                    type: array
                  initialHashedPasswordSecretRef:
                    description: |-
                      (String, Sensitive) Initial hashed password for the user, not changeable after creation. Being able to pass an initial hashed password is useful in migration scenarios.
                      Initial hashed password for the user, not changeable after creation. Being able to pass an initial hashed password is useful in migration scenarios.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  initialPasswordSecretRef:
                    description: |-
                      (String, Sensitive) Initially set password for the user, not changeable after creation
                      Initially set password for the user, not changeable after creation
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  initialSkipPasswordChange:
                    description: |-
                      (Boolean) Whether the user has to change the password on first login.
                      Whether the user has to change the password on first login.
                    type: boolean
                  isEmailVerified:
                    description: |-
                      (Boolean) Is the email verified of the user, can only be true if password of the user is set
                      Is the email verified of the user, can only be true if password of the user is set
                    type: boolean
                  isPhoneVerified:
                    description: |-
                      (Boolean) Is the phone verified of the user
                      Is the phone verified of the user
                    type: boolean
                  lastName:
                    description: |-
                      (String) Last name of the user
                      Last name of the user
                    type: string
                  metadata:
                    description: |-
                      value pairs to set on the user during creation. (see below for nested schema)
                      A list of metadata key-value pairs to set on the user during creation.
                    items:
                      properties:
                        key:
                          description: |-
                            (String) The key of the metadata entry.
                            The key of the metadata entry.
                          type: string
                        value:
                          description: |-
                            (String) The value of the metadata entry.
                            The value of the metadata entry.
                          type: string
                      type: object
                    type: array
                  nickName:
                    description: |-
                      (String) Nick name of the user
                      Nick name of the user
                    type: string
                  orgId:
                    description: |-
                      (String) ID of the organization. If not provided, the organization of the authenticated user/service account is used.
                      ID of the organization. If not provided, the organization of the authenticated user/service account is used.
                    type: string
                  phone:
                    description: |-
                      (String) Phone of the user
                      Phone of the user
                    type: string
                  preferredLanguage:
                    description: |-
                      (String) Preferred language of the user
                      Preferred language of the user
                    type: string
                  totpSecretSecretRef:
                    description: |-
                      factor authentication. Only used during creation. Useful for migration scenarios.
                      TOTP secret for two-factor authentication. Only used during creation. Useful for migration scenarios.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  userId:
                    description: |-
                      (String) The ID of this resource. Optionally set a custom unique ID. If omitted, ZITADEL will generate one.
                      The ID of this resource. Optionally set a custom unique ID. If omitted, ZITADEL will generate one.
                    type: string
                  userName:
                    description: |-
                      (String) Username
                      Username
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the user
                      Display name of the user
                    type: string
                  email:
                    description: |-
                      (String) Email of the user
                      Email of the user
                    type: string
                  firstName:
                    description: |-
                      (String) First name of the user
                      First name of the user
                    type: string
                  gender:
                    description: |-
                      (String) Gender of the user, supported values: GENDER_UNSPECIFIED, GENDER_FEMALE, GENDER_MALE, GENDER_DIVERSE
                      Gender of the user, supported values: GENDER_UNSPECIFIED, GENDER_FEMALE, GENDER_MALE, GENDER_DIVERSE
                    type: string
                  idpLinks:
                    description: |-
                      (Block List) A list of identity provider links to add to the user during creation. Useful for migration scenarios. (see below for nested schema)
                      A list of identity provider links to add to the user during creation. Useful for migration scenarios.
                    items:
                      properties:
                        idpId:
                          description: |-
                            (String) The ID of the identity provider.
                            The ID of the identity provider.
                          type: string
                        userId:
                          description: |-
                            (String) The ID of this resource. Optionally set a custom unique ID. If omitted, ZITADEL will generate one.
                            The user ID at the identity provider.
                          type: string
                        userName:
                          description: |-
                            (String) Username
                            The username at the identity provider.
                          type: string
                      type: object
                    type: array
                  initialHashedPasswordSecretRef:
                    description: |-
                      (String, Sensitive) Initial hashed password for the user, not changeable after creation. Being able to pass an initial hashed password is useful in migration scenarios.
                      Initial hashed password for the user, not changeable after creation. Being able to pass an initial hashed password is useful in migration scenarios.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  initialPasswordSecretRef:
                    description: |-
                      (String, Sensitive) Initially set password for the user, not changeable after creation
                      Initially set password for the user, not changeable after creation
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  initialSkipPasswordChange:
                    description: |-
                      (Boolean) Whether the user has to change the password on first login.
                      Whether the user has to change the password on first login.
                    type: boolean
                  isEmailVerified:
                    description: |-
                      (Boolean) Is the email verified of the user, can only be true if password of the user is set
                      Is the email verified of the user, can only be true if password of the user is set
                    type: boolean
                  isPhoneVerified:
                    description: |-
                      (Boolean) Is the phone verified of the user
                      Is the phone verified of the user
                    type: boolean
                  lastName:
                    description: |-
                      (String) Last name of the user
                      Last name of the user
                    type: string
                  metadata:
                    description: |-
                      value pairs to set on the user during creation. (see below for nested schema)
                      A list of metadata key-value pairs to set on the user during creation.
                    items:
                      properties:
                        key:
                          description: |-
                            (String) The key of the metadata entry.
                            The key of the metadata entry.
                          type: string
                        value:
                          description: |-
                            (String) The value of the metadata entry.
                            The value of the metadata entry.
                          type: string
                      type: object
                    type: array
                  nickName:
                    description: |-
                      (String) Nick name of the user
                      Nick name of the user
                    type: string
                  orgId:
                    description: |-
                      (String) ID of the organization. If not provided, the organization of the authenticated user/service account is used.
                      ID of the organization. If not provided, the organization of the authenticated user/service account is used.
                    type: string
                  phone:
                    description: |-
                      (String) Phone of the user
                      Phone of the user
                    type: string
                  preferredLanguage:
                    description: |-
                      (String) Preferred language of the user
                      Preferred language of the user
                    type: string
                  totpSecretSecretRef:
                    description: |-
                      factor authentication. Only used during creation. Useful for migration scenarios.
                      TOTP secret for two-factor authentication. Only used during creation. Useful for migration scenarios.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  userId:
                    description: |-
                      (String) The ID of this resource. Optionally set a custom unique ID. If omitted, ZITADEL will generate one.
                      The ID of this resource. Optionally set a custom unique ID. If omitted, ZITADEL will generate one.
                    type: string
                  userName:
                    description: |-
                      (String) Username
                      Username
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.email is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.email)
                || (has(self.initProvider) && has(self.initProvider.email))'
            - message: spec.forProvider.firstName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.firstName)
                || (has(self.initProvider) && has(self.initProvider.firstName))'
            - message: spec.forProvider.lastName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.lastName)
                || (has(self.initProvider) && has(self.initProvider.lastName))'
            - message: spec.forProvider.userName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.userName)
                || (has(self.initProvider) && has(self.initProvider.userName))'
          status:
            description: HumanUserStatus defines the observed state of HumanUser.
            properties:
              atProvider:
                properties:
                  displayName:
                    description: |-
                      (String) Display name of the user
                      Display name of the user
                    type: string
                  email:
                    description: |-
                      (String) Email of the user
                      Email of the user
                    type: string
                  firstName:
                    description: |-
                      (String) First name of the user
                      First name of the user
                    type: string
                  gender:
                    description: |-
                      (String) Gender of the user, supported values: GENDER_UNSPECIFIED, GENDER_FEMALE, GENDER_MALE, GENDER_DIVERSE
                      Gender of the user, supported values: GENDER_UNSPECIFIED, GENDER_FEMALE, GENDER_MALE, GENDER_DIVERSE
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  idpLinks:
                    description: |-
                      (Block List) A list of identity provider links to add to the user during creation. Useful for migration scenarios. (see below for nested schema)
                      A list of identity provider links to add to the user during creation. Useful for migration scenarios.
                    items:
                      properties:
                        idpId:
                          description: |-
                            (String) The ID of the identity provider.
                            The ID of the identity provider.
                          type: string
                        userId:
                          description: |-
                            (String) The ID of this resource. Optionally set a custom unique ID. If omitted, ZITADEL will generate one.
                            The user ID at the identity provider.
                          type: string
                        userName:
                          description: |-
                            (String) Username
                            The username at the identity provider.
                          type: string
                      type: object
                    type: array
                  initialSkipPasswordChange:
                    description: |-
                      (Boolean) Whether the user has to change the password on first login.
                      Whether the user has to change the password on first login.
                    type: boolean
                  isEmailVerified:
                    description: |-
                      (Boolean) Is the email verified of the user, can only be true if password of the user is set
                      Is the email verified of the user, can only be true if password of the user is set
                    type: boolean
                  isPhoneVerified:
                    description: |-
                      (Boolean) Is the phone verified of the user
                      Is the phone verified of the user
                    type: boolean
                  lastName:
                    description: |-
                      (String) Last name of the user
                      Last name of the user
                    type: string
                  loginNames:
                    description: |-
                      (List of String) Loginnames
                      Loginnames
                    items:
                      type: string
                    type: array
                  metadata:
                    description: |-
                      value pairs to set on the user during creation. (see below for nested schema)
                      A list of metadata key-value pairs to set on the user during creation.
                    items:
                      properties:
                        key:
                          description: |-
                            (String) The key of the metadata entry.
                            The key of the metadata entry.
                          type: string
                        value:
                          description: |-
                            (String) The value of the metadata entry.
                            The value of the metadata entry.
                          type: string
                      type: object
                    type: array
                  nickName:
                    description: |-
                      (String) Nick name of the user
                      Nick name of the user
                    type: string
                  orgId:
                    description: |-
                      (String) ID of the organization. If not provided, the organization of the authenticated user/service account is used.
                      ID of the organization. If not provided, the organization of the authenticated user/service account is used.
                    type: string
                  phone:
                    description: |-
                      (String) Phone of the user
                      Phone of the user
                    type: string
                  preferredLanguage:
                    description: |-
                      (String) Preferred language of the user
                      Preferred language of the user
                    type: string
                  preferredLoginName:
                    description: |-
                      (String) Preferred login name
                      Preferred login name
                    type: string
                  state:
                    description: |-
                      (String) State of the user
                      State of the user
                    type: string
                  userId:
                    description: |-
                      (String) The ID of this resource. Optionally set a custom unique ID. If omitted, ZITADEL will generate one.
                      The ID of this resource. Optionally set a custom unique ID. If omitted, ZITADEL will generate one.
                    type: string
                  userName:
                    description: |-
                      (String) Username
                      Username
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
