---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: features.instance.zitadel.crossplane.io
spec:
  group: instance.zitadel.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - zitadel
    kind: Features
    listKind: FeaturesList
    plural: features
    singular: features
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Features is the Schema for the Featuress API. Resource representing
          the feature flags configuration for an instance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FeaturesSpec defines the desired state of Features
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  consoleUseV2UserApi:
                    description: |-
                      (Boolean) If this is enabled the console web client will use the new User v2 API for certain calls
                      If this is enabled the console web client will use the new User v2 API for certain calls
                    type: boolean
                  debugOidcParentError:
                    description: |-
                      (Boolean) Return parent errors to OIDC clients for debugging purposes. Parent errors may contain sensitive data or unwanted details about the system status of zitadel. Only enable if really needed.
                      Return parent errors to OIDC clients for debugging purposes. Parent errors may contain sensitive data or unwanted details about the system status of zitadel. Only enable if really needed.
                    type: boolean
                  enableBackChannelLogout:
                    description: |-
                      Channel Logout to be notified in your application about terminated user sessions.
                      If the flag is enabled, you'll be able to use the OIDC Back-Channel Logout to be notified in your application about terminated user sessions.
                    type: boolean
                  improvedPerformance:
                    description: |-
                      (Set of String) Improves performance of specified execution paths. Possible values: IMPROVED_PERFORMANCE_PROJECT_GRANT, IMPROVED_PERFORMANCE_PROJECT, IMPROVED_PERFORMANCE_USER_GRANT, IMPROVED_PERFORMANCE_ORG_DOMAIN_VERIFIED
                      Improves performance of specified execution paths. Possible values: IMPROVED_PERFORMANCE_PROJECT_GRANT, IMPROVED_PERFORMANCE_PROJECT, IMPROVED_PERFORMANCE_USER_GRANT, IMPROVED_PERFORMANCE_ORG_DOMAIN_VERIFIED
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  loginDefaultOrg:
                    description: |-
                      (Boolean) The login UI will use the settings of the default org (and not from the instance) if no organization context is set
                      The login UI will use the settings of the default org (and not from the instance) if no organization context is set
                    type: boolean
                  loginV2:
                    description: |-
                      (Block List, Max: 1) Specify the login UI for all users and applications regardless of their preference. (see below for nested schema)
                      Specify the login UI for all users and applications regardless of their preference.
                    items:
                      properties:
                        baseUri:
                          description: |-
                            (String) Optionally specify a base uri of the login UI. If unspecified the default URI will be used.
                            Optionally specify a base uri of the login UI. If unspecified the default URI will be used.
                          type: string
                        required:
                          description: |-
                            (Boolean) Require that all users must use the new login UI. If enabled, all users will be redirected to the login V2 regardless of the application's preference.
                            Require that all users must use the new login UI. If enabled, all users will be redirected to the login V2 regardless of the application's preference.
                          type: boolean
                      type: object
                    type: array
                  oidcSingleV1SessionTermination:
                    description: |-
                      (Boolean) If the flag is enabled, you'll be able to terminate a single session from the login UI by providing an id_token with a sid claim as id_token_hint on the end_session endpoint. Note that currently all sessions from the same user agent (browser) are terminated in the login UI. Sessions managed through the Session API already allow the termination of single sessions.
                      If the flag is enabled, you'll be able to terminate a single session from the login UI by providing an id_token with a `sid` claim as id_token_hint on the end_session endpoint. Note that currently all sessions from the same user agent (browser) are terminated in the login UI. Sessions managed through the Session API already allow the termination of single sessions.
                    type: boolean
                  oidcTokenExchange:
                    description: |-
                      type:token-exchange grant type for the OIDC token endpoint. Token exchange can be used to request tokens with a lesser scope or impersonate other users. See the security policy to allow impersonation on an instance.
                      Enable the experimental `urn:ietf:params:oauth:grant-type:token-exchange` grant type for the OIDC token endpoint. Token exchange can be used to request tokens with a lesser scope or impersonate other users. See the security policy to allow impersonation on an instance.
                    type: boolean
                  permissionCheckV2:
                    description: |-
                      (Boolean) Enable a newer, more performant, permission check used for v2 and v3 resource based APIs.
                      Enable a newer, more performant, permission check used for v2 and v3 resource based APIs.
                    type: boolean
                  userSchema:
                    description: |-
                      (Boolean) User Schemas allow to manage data schemas of user. If the flag is enabled, you'll be able to use the new API and its features. Note that it is still in an early stage.
                      User Schemas allow to manage data schemas of user. If the flag is enabled, you'll be able to use the new API and its features. Note that it is still in an early stage.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  consoleUseV2UserApi:
                    description: |-
                      (Boolean) If this is enabled the console web client will use the new User v2 API for certain calls
                      If this is enabled the console web client will use the new User v2 API for certain calls
                    type: boolean
                  debugOidcParentError:
                    description: |-
                      (Boolean) Return parent errors to OIDC clients for debugging purposes. Parent errors may contain sensitive data or unwanted details about the system status of zitadel. Only enable if really needed.
                      Return parent errors to OIDC clients for debugging purposes. Parent errors may contain sensitive data or unwanted details about the system status of zitadel. Only enable if really needed.
                    type: boolean
                  enableBackChannelLogout:
                    description: |-
                      Channel Logout to be notified in your application about terminated user sessions.
                      If the flag is enabled, you'll be able to use the OIDC Back-Channel Logout to be notified in your application about terminated user sessions.
                    type: boolean
                  improvedPerformance:
                    description: |-
                      (Set of String) Improves performance of specified execution paths. Possible values: IMPROVED_PERFORMANCE_PROJECT_GRANT, IMPROVED_PERFORMANCE_PROJECT, IMPROVED_PERFORMANCE_USER_GRANT, IMPROVED_PERFORMANCE_ORG_DOMAIN_VERIFIED
                      Improves performance of specified execution paths. Possible values: IMPROVED_PERFORMANCE_PROJECT_GRANT, IMPROVED_PERFORMANCE_PROJECT, IMPROVED_PERFORMANCE_USER_GRANT, IMPROVED_PERFORMANCE_ORG_DOMAIN_VERIFIED
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  loginDefaultOrg:
                    description: |-
                      (Boolean) The login UI will use the settings of the default org (and not from the instance) if no organization context is set
                      The login UI will use the settings of the default org (and not from the instance) if no organization context is set
                    type: boolean
                  loginV2:
                    description: |-
                      (Block List, Max: 1) Specify the login UI for all users and applications regardless of their preference. (see below for nested schema)
                      Specify the login UI for all users and applications regardless of their preference.
                    items:
                      properties:
                        baseUri:
                          description: |-
                            (String) Optionally specify a base uri of the login UI. If unspecified the default URI will be used.
                            Optionally specify a base uri of the login UI. If unspecified the default URI will be used.
                          type: string
                        required:
                          description: |-
                            (Boolean) Require that all users must use the new login UI. If enabled, all users will be redirected to the login V2 regardless of the application's preference.
                            Require that all users must use the new login UI. If enabled, all users will be redirected to the login V2 regardless of the application's preference.
                          type: boolean
                      type: object
                    type: array
                  oidcSingleV1SessionTermination:
                    description: |-
                      (Boolean) If the flag is enabled, you'll be able to terminate a single session from the login UI by providing an id_token with a sid claim as id_token_hint on the end_session endpoint. Note that currently all sessions from the same user agent (browser) are terminated in the login UI. Sessions managed through the Session API already allow the termination of single sessions.
                      If the flag is enabled, you'll be able to terminate a single session from the login UI by providing an id_token with a `sid` claim as id_token_hint on the end_session endpoint. Note that currently all sessions from the same user agent (browser) are terminated in the login UI. Sessions managed through the Session API already allow the termination of single sessions.
                    type: boolean
                  oidcTokenExchange:
                    description: |-
                      type:token-exchange grant type for the OIDC token endpoint. Token exchange can be used to request tokens with a lesser scope or impersonate other users. See the security policy to allow impersonation on an instance.
                      Enable the experimental `urn:ietf:params:oauth:grant-type:token-exchange` grant type for the OIDC token endpoint. Token exchange can be used to request tokens with a lesser scope or impersonate other users. See the security policy to allow impersonation on an instance.
                    type: boolean
                  permissionCheckV2:
                    description: |-
                      (Boolean) Enable a newer, more performant, permission check used for v2 and v3 resource based APIs.
                      Enable a newer, more performant, permission check used for v2 and v3 resource based APIs.
                    type: boolean
                  userSchema:
                    description: |-
                      (Boolean) User Schemas allow to manage data schemas of user. If the flag is enabled, you'll be able to use the new API and its features. Note that it is still in an early stage.
                      User Schemas allow to manage data schemas of user. If the flag is enabled, you'll be able to use the new API and its features. Note that it is still in an early stage.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: FeaturesStatus defines the observed state of Features.
            properties:
              atProvider:
                properties:
                  consoleUseV2UserApi:
                    description: |-
                      (Boolean) If this is enabled the console web client will use the new User v2 API for certain calls
                      If this is enabled the console web client will use the new User v2 API for certain calls
                    type: boolean
                  debugOidcParentError:
                    description: |-
                      (Boolean) Return parent errors to OIDC clients for debugging purposes. Parent errors may contain sensitive data or unwanted details about the system status of zitadel. Only enable if really needed.
                      Return parent errors to OIDC clients for debugging purposes. Parent errors may contain sensitive data or unwanted details about the system status of zitadel. Only enable if really needed.
                    type: boolean
                  enableBackChannelLogout:
                    description: |-
                      Channel Logout to be notified in your application about terminated user sessions.
                      If the flag is enabled, you'll be able to use the OIDC Back-Channel Logout to be notified in your application about terminated user sessions.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  improvedPerformance:
                    description: |-
                      (Set of String) Improves performance of specified execution paths. Possible values: IMPROVED_PERFORMANCE_PROJECT_GRANT, IMPROVED_PERFORMANCE_PROJECT, IMPROVED_PERFORMANCE_USER_GRANT, IMPROVED_PERFORMANCE_ORG_DOMAIN_VERIFIED
                      Improves performance of specified execution paths. Possible values: IMPROVED_PERFORMANCE_PROJECT_GRANT, IMPROVED_PERFORMANCE_PROJECT, IMPROVED_PERFORMANCE_USER_GRANT, IMPROVED_PERFORMANCE_ORG_DOMAIN_VERIFIED
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  loginDefaultOrg:
                    description: |-
                      (Boolean) The login UI will use the settings of the default org (and not from the instance) if no organization context is set
                      The login UI will use the settings of the default org (and not from the instance) if no organization context is set
                    type: boolean
                  loginV2:
                    description: |-
                      (Block List, Max: 1) Specify the login UI for all users and applications regardless of their preference. (see below for nested schema)
                      Specify the login UI for all users and applications regardless of their preference.
                    items:
                      properties:
                        baseUri:
                          description: |-
                            (String) Optionally specify a base uri of the login UI. If unspecified the default URI will be used.
                            Optionally specify a base uri of the login UI. If unspecified the default URI will be used.
                          type: string
                        required:
                          description: |-
                            (Boolean) Require that all users must use the new login UI. If enabled, all users will be redirected to the login V2 regardless of the application's preference.
                            Require that all users must use the new login UI. If enabled, all users will be redirected to the login V2 regardless of the application's preference.
                          type: boolean
                      type: object
                    type: array
                  oidcSingleV1SessionTermination:
                    description: |-
                      (Boolean) If the flag is enabled, you'll be able to terminate a single session from the login UI by providing an id_token with a sid claim as id_token_hint on the end_session endpoint. Note that currently all sessions from the same user agent (browser) are terminated in the login UI. Sessions managed through the Session API already allow the termination of single sessions.
                      If the flag is enabled, you'll be able to terminate a single session from the login UI by providing an id_token with a `sid` claim as id_token_hint on the end_session endpoint. Note that currently all sessions from the same user agent (browser) are terminated in the login UI. Sessions managed through the Session API already allow the termination of single sessions.
                    type: boolean
                  oidcTokenExchange:
                    description: |-
                      type:token-exchange grant type for the OIDC token endpoint. Token exchange can be used to request tokens with a lesser scope or impersonate other users. See the security policy to allow impersonation on an instance.
                      Enable the experimental `urn:ietf:params:oauth:grant-type:token-exchange` grant type for the OIDC token endpoint. Token exchange can be used to request tokens with a lesser scope or impersonate other users. See the security policy to allow impersonation on an instance.
                    type: boolean
                  permissionCheckV2:
                    description: |-
                      (Boolean) Enable a newer, more performant, permission check used for v2 and v3 resource based APIs.
                      Enable a newer, more performant, permission check used for v2 and v3 resource based APIs.
                    type: boolean
                  userSchema:
                    description: |-
                      (Boolean) User Schemas allow to manage data schemas of user. If the flag is enabled, you'll be able to use the new API and its features. Note that it is still in an early stage.
                      User Schemas allow to manage data schemas of user. If the flag is enabled, you'll be able to use the new API and its features. Note that it is still in an early stage.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
